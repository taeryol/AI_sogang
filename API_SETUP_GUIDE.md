# 🔑 API 설정 완벽 가이드

## 목차
1. [OpenAI API 키 발급 및 설정](#1-openai-api-키-발급-및-설정)
2. [Vector DB 선택 가이드](#2-vector-db-선택-가이드)
3. [Simple Vector DB 설정](#3-simple-vector-db-설정)
4. [Pinecone Vector DB 설정](#4-pinecone-vector-db-설정)
5. [비용 및 성능 비교](#5-비용-및-성능-비교)

---

## 1. OpenAI API 키 발급 및 설정

### 1.1 OpenAI 계정 생성

**단계별 가이드:**

1. **OpenAI 웹사이트 접속**
   - URL: https://platform.openai.com/signup
   - "Sign up" 버튼 클릭

2. **계정 생성 방법 선택**
   - Google 계정으로 가입 (추천)
   - Microsoft 계정으로 가입
   - 이메일로 가입

3. **전화번호 인증**
   - 한국 전화번호 입력 가능
   - SMS로 인증 코드 받기
   - 인증 코드 입력

### 1.2 API 키 발급

1. **OpenAI Dashboard 접속**
   - 로그인 후 https://platform.openai.com 접속
   - 좌측 메뉴에서 "API keys" 클릭

2. **새 API 키 생성**
   ```
   1. "Create new secret key" 버튼 클릭
   2. 키 이름 입력 (예: "AI-KMS-Production")
   3. "Create secret key" 클릭
   4. 생성된 키 복사 (⚠️ 한 번만 표시됨!)
   ```

3. **API 키 형식**
   ```
   sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
   - 항상 `sk-`로 시작
   - 약 51자 길이
   - 영문자, 숫자, 하이픈 조합

**⚠️ 중요 주의사항:**
- API 키는 생성 직후 **단 한 번만** 표시됩니다
- 반드시 안전한 곳에 복사해서 저장하세요
- 키를 분실하면 새로 생성해야 합니다
- 키가 노출되면 즉시 삭제하고 새로 발급받으세요

### 1.3 결제 방법 설정 (필수)

OpenAI API는 사용한 만큼 과금되는 **종량제**입니다.

**단계:**

1. **결제 설정 페이지 접속**
   - Dashboard > Settings > Billing
   - 또는 https://platform.openai.com/account/billing/overview

2. **결제 수단 등록**
   ```
   1. "Add payment method" 클릭
   2. 신용카드 정보 입력
      - VISA, MasterCard, American Express 사용 가능
      - 한국 발행 카드 사용 가능
   3. 결제 정보 저장
   ```

3. **사용 한도 설정 (선택사항)**
   ```
   1. "Usage limits" 탭 클릭
   2. "Hard limit" 설정 (예: $10)
   3. 설정한 금액 초과 시 API 자동 중단
   ```

**💰 비용 예상:**
- GPT-4: 입력 $0.03/1K tokens, 출력 $0.06/1K tokens
- GPT-3.5 Turbo: 입력 $0.0005/1K tokens, 출력 $0.0015/1K tokens
- Embedding (text-embedding-3-small): $0.00002/1K tokens

**참고**: 1,000 tokens ≈ 750 단어 ≈ 한글 500자

### 1.4 관리자 패널에 API 키 입력

**방법:**

1. **관리자 패널 접속**
   ```
   https://a85a98fe.webapp-31i.pages.dev/admin
   ```

2. **API 설정 탭 이동**
   ```
   관리자 패널 > "API 설정" 탭 클릭
   ```

3. **OpenAI 설정 입력**
   ```
   1. "API Key" 필드에 복사한 키 붙여넣기
   2. "GPT 모델" 선택
      - GPT-4: 가장 강력 (추천)
      - GPT-4 Turbo: 빠르고 저렴
      - GPT-3.5 Turbo: 경제적
   3. "임베딩 모델" 선택
      - text-embedding-3-small (추천)
   4. "저장" 버튼 클릭
   ```

4. **설정 확인**
   ```
   1. 메인 페이지로 이동
   2. 질문 입력 후 전송
   3. AI 답변이 정상적으로 생성되는지 확인
   ```

### 1.5 API 사용량 모니터링

**확인 방법:**

1. **Usage Dashboard 접속**
   - https://platform.openai.com/usage
   
2. **확인 가능한 정보**
   - 일별 사용량 그래프
   - 모델별 비용
   - 총 사용 금액
   - 예상 월 비용

3. **알림 설정**
   ```
   Settings > Billing > Email preferences
   - 사용량 80% 도달 시 알림
   - 사용량 100% 도달 시 알림
   ```

---

## 2. Vector DB 선택 가이드

### 2.1 Vector DB란?

**개념:**
- AI가 문서를 이해하기 위해 텍스트를 **숫자 벡터**로 변환합니다
- 이 벡터들을 저장하고 검색하는 데이터베이스가 Vector DB입니다
- 사용자 질문과 가장 유사한 문서를 빠르게 찾기 위해 사용합니다

**작동 원리:**
```
1. 문서 업로드
   ↓
2. OpenAI Embedding API로 벡터 변환
   예: "인공지능" → [0.123, -0.456, 0.789, ...]
   ↓
3. Vector DB에 저장
   ↓
4. 사용자 질문도 벡터로 변환
   ↓
5. Vector DB에서 유사한 벡터 검색
   ↓
6. 관련 문서 찾기
```

### 2.2 Simple vs Pinecone 비교

#### Simple Vector DB (인메모리)

**특징:**
- ✅ 설정 없이 즉시 사용 가능
- ✅ 추가 비용 없음 (완전 무료)
- ✅ 간단한 구조로 이해하기 쉬움
- ✅ 개발 및 테스트에 최적
- ❌ 서버 재시작 시 데이터 소실
- ❌ 메모리에만 저장 (영구 저장 X)
- ❌ 대용량 데이터 처리 제한
- ❌ 검색 속도 느림 (데이터 많을 때)

**언제 사용?**
- 🧪 프로토타입 개발 중
- 📚 문서 수가 적음 (10개 이하)
- 💻 로컬 개발 및 테스트
- 💰 비용을 들이고 싶지 않음

**제한사항:**
- 최대 약 100개 문서 권장
- 서버 재시작마다 재업로드 필요
- 동시 사용자 많으면 느려짐

#### Pinecone (프로덕션 Vector DB)

**특징:**
- ✅ 영구 저장 (서버 재시작해도 유지)
- ✅ 초고속 검색 (밀리초 단위)
- ✅ 대용량 데이터 지원 (수백만 개)
- ✅ 자동 확장 및 백업
- ✅ 프로덕션 환경에 최적화
- ❌ 별도 계정 및 API 키 필요
- ❌ 월 사용료 발생 (무료 플랜 있음)
- ❌ 설정이 다소 복잡

**언제 사용?**
- 🚀 실제 서비스 운영 시
- 📚 문서 수가 많음 (100개 이상)
- 👥 동시 사용자가 많음
- 🔒 데이터 영구 보존 필요
- ⚡ 빠른 검색 속도 필요

**장점:**
- 수백만 개 벡터도 빠르게 검색
- 99.9% 가용성 보장
- 자동 백업 및 복구
- 클라우드 기반으로 안정적

### 2.3 선택 가이드 플로우차트

```
시작
 │
 ├─ 개발/테스트 중인가? ───────────────────────> Simple 선택
 │    YES
 │
 ├─ 문서가 10개 미만인가? ─────────────────────> Simple 선택
 │    YES
 │
 ├─ 비용을 절약하고 싶은가? ───────────────────> Simple 선택
 │    YES (무료로 시작)
 │
 ├─ 실제 서비스를 운영 중인가? ────────────────> Pinecone 선택
 │    YES
 │
 ├─ 문서가 100개 이상인가? ────────────────────> Pinecone 선택
 │    YES
 │
 ├─ 동시 사용자가 많은가? ─────────────────────> Pinecone 선택
 │    YES
 │
 └─ 데이터 영구 보존이 필요한가? ──────────────> Pinecone 선택
      YES
```

**권장 전략:**
```
1단계: Simple로 시작 (개발 및 프로토타입)
   ↓
2단계: 기능 검증 및 테스트
   ↓
3단계: 문서가 늘어나면 Pinecone으로 전환
   ↓
4단계: 실제 서비스 운영
```

---

## 3. Simple Vector DB 설정

### 3.1 설정 방법

**매우 간단합니다!**

1. **관리자 패널 접속**
   ```
   https://a85a98fe.webapp-31i.pages.dev/admin
   ```

2. **API 설정 탭 이동**
   ```
   "API 설정" 탭 클릭
   ```

3. **Vector DB 타입 선택**
   ```
   "Vector DB 타입" 드롭다운에서
   "Simple (인메모리 - 개발용)" 선택
   ```

4. **저장**
   ```
   "저장" 버튼 클릭
   ```

**끝!** 추가 설정 없이 바로 사용 가능합니다.

### 3.2 동작 확인

1. **문서 업로드**
   ```
   메인 페이지 > 문서 관리 > 파일 업로드
   ```

2. **질문 테스트**
   ```
   메인 페이지 > 질문 입력 > 전송
   AI가 업로드한 문서에서 답변 생성
   ```

3. **제한사항 이해**
   ```
   ⚠️ 서버 재시작 시 업로드한 문서 사라짐
   ⚠️ 다시 문서 업로드 필요
   ```

---

## 4. Pinecone Vector DB 설정

### 4.1 Pinecone 계정 생성

**단계별 가이드:**

1. **Pinecone 웹사이트 접속**
   - URL: https://www.pinecone.io/
   - "Start Free" 또는 "Sign Up" 클릭

2. **계정 생성**
   ```
   방법 1: Google 계정 연동 (추천)
   방법 2: 이메일로 가입
   ```

3. **이메일 인증**
   - 가입한 이메일로 인증 링크 전송
   - 링크 클릭하여 인증 완료

### 4.2 프로젝트 생성

1. **대시보드 접속**
   - 로그인 후 https://app.pinecone.io/

2. **프로젝트 생성 (자동)**
   - 첫 로그인 시 자동으로 기본 프로젝트 생성
   - 프로젝트 이름 확인

### 4.3 Index 생성

**Index란?** 벡터를 저장하는 데이터베이스 공간입니다.

**생성 방법:**

1. **Index 페이지 이동**
   ```
   좌측 메뉴 > "Indexes" 클릭
   ```

2. **새 Index 생성**
   ```
   "Create Index" 버튼 클릭
   ```

3. **Index 설정 입력**
   ```
   ┌─────────────────────────────────────────┐
   │ Index name: kms-embeddings              │
   │ Dimensions: 1536                        │
   │   (text-embedding-3-small 모델 차원수)  │
   │ Metric: cosine                          │
   │   (유사도 측정 방법)                     │
   │ Cloud: AWS                              │
   │ Region: us-west-2 (또는 가까운 지역)     │
   └─────────────────────────────────────────┘
   ```

4. **플랜 선택**
   ```
   Starter Plan (무료)
   - 1개 Index
   - 100,000 벡터
   - 월 무료
   
   Standard Plan ($70/월)
   - 5개 Index
   - 무제한 벡터
   - 더 빠른 속도
   ```

5. **생성 완료**
   ```
   "Create Index" 버튼 클릭
   몇 분 대기 (Index 준비 중)
   상태가 "Ready"가 되면 사용 가능
   ```

### 4.4 API 키 발급

1. **API Keys 페이지 이동**
   ```
   좌측 메뉴 > "API Keys" 클릭
   ```

2. **API 키 복사**
   ```
   기본 API 키가 이미 생성되어 있음
   "Copy" 버튼으로 복사
   ```

3. **API 키 형식**
   ```
   pcsk-xxxxx-xxxxx-xxxxx-xxxxx
   ```
   - 항상 `pcsk-`로 시작
   - 하이픈으로 구분된 여러 부분

### 4.5 Environment 확인

1. **Project 설정 확인**
   ```
   좌측 메뉴 > "Settings" 클릭
   ```

2. **Environment 복사**
   ```
   예시:
   - us-west1-gcp
   - us-east-1-aws
   - europe-west1-gcp
   
   본인의 프로젝트 environment 복사
   ```

### 4.6 관리자 패널에 Pinecone 설정 입력

1. **관리자 패널 접속**
   ```
   https://a85a98fe.webapp-31i.pages.dev/admin
   ```

2. **API 설정 탭 이동**
   ```
   "API 설정" 탭 클릭
   ```

3. **Vector DB 설정**
   ```
   1. "Vector DB 타입": Pinecone 선택
   
   2. "Pinecone API Key": 
      - 복사한 API 키 붙여넣기
      - 예: pcsk-xxxxx-xxxxx-xxxxx-xxxxx
   
   3. "Environment":
      - 복사한 environment 입력
      - 예: us-west1-gcp
   
   4. "Index Name":
      - 생성한 Index 이름 입력
      - 예: kms-embeddings
   
   5. "저장" 버튼 클릭
   ```

### 4.7 동작 확인

1. **문서 업로드**
   ```
   메인 페이지 > 문서 관리 > 파일 업로드
   Pinecone에 벡터 자동 저장
   ```

2. **Pinecone 대시보드 확인**
   ```
   Pinecone Console > Indexes > kms-embeddings
   Vector 개수 증가 확인
   ```

3. **질문 테스트**
   ```
   메인 페이지 > 질문 입력
   Pinecone에서 관련 문서 검색
   빠른 응답 확인
   ```

4. **영구 저장 확인**
   ```
   서버 재시작 후에도
   문서 재업로드 없이 바로 검색 가능
   ```

---

## 5. 비용 및 성능 비교

### 5.1 비용 비교

#### Simple Vector DB
```
월 비용: $0 (완전 무료)

추가 비용 없음
서버 자원만 사용
```

#### Pinecone
```
Starter Plan (무료)
- 1개 Index
- 최대 100,000 벡터
- 월 $0

Standard Plan
- 시작 가격: $70/월
- 5개 Index
- 무제한 벡터
- Pod 타입에 따라 추가 비용

예시: 10,000개 문서 (1536 차원)
- Starter Plan: 무료 (10만 개 이하)
- Standard s1: $70/월
```

**참고**: 대부분의 프로토타입은 Starter Plan으로 충분합니다.

### 5.2 성능 비교

| 기준 | Simple | Pinecone |
|------|--------|----------|
| **검색 속도 (100개)** | ~50ms | ~10ms |
| **검색 속도 (10,000개)** | ~500ms | ~15ms |
| **검색 속도 (100,000개)** | ❌ 불가 | ~20ms |
| **동시 검색** | 느림 | 빠름 |
| **메모리 사용** | 높음 | 낮음 |
| **데이터 영구성** | ❌ | ✅ |
| **자동 백업** | ❌ | ✅ |

### 5.3 확장성 비교

#### Simple Vector DB
```
문서 10개: ✅ 아주 좋음
문서 50개: ✅ 좋음
문서 100개: ⚠️ 느려짐
문서 1,000개: ❌ 사용 불가
```

#### Pinecone
```
문서 10개: ✅ 오버스펙
문서 100개: ✅ 최적
문서 1,000개: ✅ 완벽
문서 100,000개: ✅ 매우 빠름
문서 1,000,000개: ✅ 가능 (유료 플랜)
```

### 5.4 실제 사용 시나리오별 권장

#### 시나리오 1: 개인 프로젝트
```
문서: ~10개
사용자: 본인만
목적: 학습 및 실험

권장: Simple Vector DB
이유: 무료, 설정 간단, 충분한 성능
```

#### 시나리오 2: 소규모 팀
```
문서: 50-100개
사용자: 5-10명
목적: 내부 지식 공유

권장: Simple 또는 Pinecone Starter
이유: 
- Simple: 비용 절감, 문서 적음
- Pinecone: 데이터 보존, 안정성
```

#### 시나리오 3: 스타트업
```
문서: 100-1,000개
사용자: 50-100명
목적: 고객 지원, 제품 문서

권장: Pinecone Standard
이유: 빠른 속도, 안정성, 확장성
```

#### 시나리오 4: 엔터프라이즈
```
문서: 10,000개+
사용자: 1,000명+
목적: 기업 지식 관리

권장: Pinecone Enterprise
이유: 대용량, 보안, SLA 보장
```

---

## 6. 문제 해결 (Troubleshooting)

### 6.1 OpenAI API 관련

**문제: API 키 인증 실패**
```
에러: "Invalid API key"

해결:
1. API 키를 다시 복사
2. 공백이나 개행 문자 없는지 확인
3. sk-로 시작하는지 확인
4. OpenAI Dashboard에서 키가 활성화되었는지 확인
```

**문제: 결제 방법 없음**
```
에러: "You exceeded your current quota"

해결:
1. OpenAI Dashboard > Billing
2. 결제 수단 추가
3. 크레딧 충전 (최소 $5)
```

**문제: Rate Limit 초과**
```
에러: "Rate limit exceeded"

해결:
1. 잠시 대기 (1분 후 재시도)
2. 요청 빈도 줄이기
3. 유료 플랜 업그레이드 고려
```

### 6.2 Pinecone 관련

**문제: Index 생성 실패**
```
에러: "Index already exists"

해결:
1. 다른 이름으로 Index 생성
2. 또는 기존 Index 사용
```

**문제: Dimension 불일치**
```
에러: "Dimension mismatch"

해결:
1. Index 생성 시 Dimensions: 1536 설정
2. text-embedding-3-small 사용 시 1536
3. text-embedding-3-large 사용 시 3072
```

**문제: API 연결 실패**
```
에러: "Failed to connect to Pinecone"

해결:
1. API Key 재확인
2. Environment 정확히 입력
3. Index 이름 확인
4. Index가 "Ready" 상태인지 확인
```

---

## 7. 추가 리소스

### 공식 문서
- OpenAI API: https://platform.openai.com/docs
- OpenAI Pricing: https://openai.com/api/pricing/
- Pinecone Docs: https://docs.pinecone.io/
- Pinecone Pricing: https://www.pinecone.io/pricing/

### 유용한 링크
- OpenAI Playground: https://platform.openai.com/playground
- Pinecone Console: https://app.pinecone.io/
- OpenAI Usage Dashboard: https://platform.openai.com/usage

### 커뮤니티
- OpenAI Community: https://community.openai.com/
- Pinecone Community: https://community.pinecone.io/

---

## 8. 빠른 체크리스트

### ✅ OpenAI 설정 체크리스트
- [ ] OpenAI 계정 생성
- [ ] 결제 방법 등록
- [ ] API 키 발급
- [ ] API 키 복사 및 저장
- [ ] 관리자 패널에 입력
- [ ] 테스트 질문으로 확인

### ✅ Simple Vector DB 체크리스트
- [ ] 관리자 패널 접속
- [ ] API 설정 탭 이동
- [ ] "Simple" 선택
- [ ] 저장
- [ ] 문서 업로드 테스트

### ✅ Pinecone 설정 체크리스트
- [ ] Pinecone 계정 생성
- [ ] Index 생성 (1536 dimensions)
- [ ] API 키 복사
- [ ] Environment 확인
- [ ] 관리자 패널에 모든 정보 입력
- [ ] 저장
- [ ] 문서 업로드 및 검색 테스트
- [ ] Pinecone Console에서 벡터 확인

---

**마지막 업데이트**: 2025-11-10  
**작성자**: AI Assistant  
**문의**: 추가 질문이 있으시면 언제든 물어보세요!
